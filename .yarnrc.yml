nodeLinker: node-modules

yarnPath: .yarn/releases/yarn-3.6.4.cjs

# Keep dependency installs stable on small production hosts and retain archives
# between Docker builds through the BuildKit cache mount.
enableGlobalCache: true
httpTimeout: 120000
networkConcurrency: 8
