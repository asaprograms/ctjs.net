import ApplyForm from "./ApplyForm";

export const metadata = {
  title: "Submit a module | CTJS",
  description: "Apply to publish a ChatTriggers module on the ctjs.net registry.",
};

export default function ApplyPage() {
  return <ApplyForm />;
}
